{
    "rootDirectory": "backend",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "pip install -r requirements.txt"
    },
    "start": {
        "startCommand": "python app.py"
    }
}
